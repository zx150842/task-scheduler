package com.dts.executor;

import com.google.common.collect.Lists;

import org.junit.Test;

import java.io.File;
import java.net.URL;
import java.util.ArrayList;
import java.util.List;

/**
 * @author zhangxin
 */
public class ScannerTest {

//  @Test
//  public void test() {
//    String packageName = "";
//    List<Class<?>> list = find(packageName);
//    for (Class<?> c : list) {
//      System.out.println(c);
//    }
//  }
//
//  public static List<Class<?>> find(String packageName) {
////    String scannedPath = packageName.replace(".", "/");
////    URL scannedUrl = Thread.currentThread().getContextClassLoader().getResource(scannedPath);
////    if (scannedUrl == null) {
////      throw new IllegalArgumentException("error");
////    }
//    File scannedDir = new File("D:\\workspace\\antispam-extractor\\build\\libs\\project.jar!/BOOT-INF/classes!/com/sogou/daohang/task");
//    List<Class<?>> classes = Lists.newArrayList();
//    for (File file : scannedDir.listFiles()) {
//      classes.addAll(find(file, packageName));
//    }
//    return classes;
//  }
}
